I wanted to extend the change_list admin template

but it wouldn't extend from django's it could never find the path

so instead I made a copy of the templates I wanted to extend from django-contrib 
and moved them into this admin_copies directory, and admin templates extend these    